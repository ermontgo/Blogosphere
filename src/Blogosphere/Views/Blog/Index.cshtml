@model IEnumerable<Blogosphere.Models.Blog>

@{
    ViewBag.Title = "Index";
}

<h2>Posts</h2>

<div id="posts">
    @if (Model.Count() == 0) {
        <p>There are no blog posts yet.</p>
    }
    @foreach (var item in Model)
    {
        Html.RenderPartial("PartialBlog", item);
    }
</div>
